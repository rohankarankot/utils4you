import React from 'react';

export default function Head({ params }: { params: { slug: string } }){
  return (
    <>
      {/* Title & meta will be set by Seo component on the page */}
    </>
  );
}
